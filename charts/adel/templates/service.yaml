apiVersion: v1
kind: Service
metadata:
  name: {{ include "adel.fullname" . }}
  labels:
    {{- include "adel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adel.selectorLabels" . | nindent 4 }}
